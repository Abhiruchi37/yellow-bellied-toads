# CVAT Annotation Export

This folder stores the CVAT Ultralytics YOLO Segmentation export used by the belly pipeline.

- `labels/train/` — polygon annotation `.txt` files exported from CVAT.
- Keep annotation files here; visual screenshots of annotations belong in `../images/annotations/`.
