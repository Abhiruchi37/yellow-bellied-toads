# YOLO Prediction Examples

This folder contains selected examples of predictions produced by the trained YOLOv8n segmentation model on test images.

The blue segmentation region shows the detected combined belly + throat area.

The model was applied using a confidence threshold of 0.5.

Only selected examples are included here instead of the complete prediction output. The corresponding cropped regions are available in the `../crop_examples/` folder.
